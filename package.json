{
  "name": "aranis-api",
  "version": "1.0.0",
  "private": true,
  "description": "Version holder for the Aranis API contract. This repository ships no JavaScript of its own; the number lives here because changesets reads package.json, and scripts/sync-version.mjs copies it into the three places that actually declare it.",
  "scripts": {
    "changeset": "changeset",
    "sync-version": "node scripts/sync-version.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.1"
  }
}
